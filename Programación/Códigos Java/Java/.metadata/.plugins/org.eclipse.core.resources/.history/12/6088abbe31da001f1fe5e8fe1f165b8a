package examen;

import java.util.Scanner;

public class Texto {
	public static void main(String[] args) {
		Scanner input = new Scanner(System.in);
		Texto r = new Texto();
		System.out.println("RECUERDA. Cada palabra debe estar separada por comas");
		System.out.println("Intorduce tu texto: ");
		String texto = input.nextLine();
		String [] letras = new String [texto.length()];
		for(int i =0; i<texto.length(); i++) {
			letras[i] = texto.valueOf(i);
			System.out.println(letras[i]);

		}
		/*int longitud = texto.length();
		System.out.println(texto);
		int consecutivas = r.Comprobador(texto, longitud);
		System.out.println(consecutivas);
		input.close();*/
	}
	
	

	/*int Comprobador(String texto, int longitud) {
		int consecutivas = 0;
		int puntos = 0;
		for (int i = 0; i < longitud; i++) {
			if (texto.valueOf(i).equals("a") || texto.valueOf(i).equals("e") || texto.valueOf(i).equals("i") || texto.valueOf(i).equals("o") || texto.valueOf(i).equals("u")) {
				puntos++;
				if(puntos == 2) {
					consecutivas++;
					puntos = 0;
				}
				
			}
		}

		return consecutivas;

	}*/

}
